export class Ingredient {
    // public name: string;
    // public amount: number;

    constructor(public name: string, public amount: number) {
        //putting public before the argument names above automatically creates internal values
        // this.name = name;
        // this.amount = amount;
    }
}